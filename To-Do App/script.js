// Function to add a task
function addTask() {
    // Get the value from the input field
    // Validate the input to ensure it’s not empty
    // Add the task to the tasks array as an object
    // Clear the input field
    // Call the function to update the displayed task list
}

// Function to render tasks
function renderTasks() {
    // Select the element that will display the task list
    // Clear the current list to avoid duplicates

    // Loop through each task in the tasks array
        // Use template literals to create a list item for each task
        // Add buttons for completing and removing the task
}

// Function to mark a task as complete
function markComplete(index) {
    // Toggle the isCompleted property of the task at the given index
    // Call the renderTasks function to update the display
}

// Function to remove a task
function removeTask(index) {
    // Remove the task from the tasks array using splice
    // Call the renderTasks function to update the display
}

// Add event listener to the "Add Task" button
// When clicked, call the addTask function
